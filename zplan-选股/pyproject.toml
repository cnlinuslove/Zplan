[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "zplan-pick"
version = "0.1.0"
description = "Z-Plan 选股 Agent"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
