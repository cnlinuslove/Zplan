# 可选：本机安装 direnv 并在 ~/.zshrc 写入 eval "$(direnv hook zsh)" 后，
# 在仓库根执行一次 `direnv allow`，之后 cd 到此目录会自动把 python 指到 .venv。
if [[ -d .venv ]]; then
  export VIRTUAL_ENV="${PWD}/.venv"
  PATH_add "${VIRTUAL_ENV}/bin"
fi
